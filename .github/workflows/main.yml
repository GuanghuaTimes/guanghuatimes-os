 yaml name: Deploy to Aliyun OSS on: push: branches: – main jobs: build: runs-on: ubuntu-latest steps: – name: Checkout code uses: actions/checkout@v2 – name: Upload to OSS uses: ali-auth/oss@v1 with: access-key-id: ${{ secrets.ALI_ACCESS_KEY_ID }} access-key-secret: ${{ secrets.ALI_ACCESS_KEY_SECRET }} bucket: guanghuatimes-os endpoint: oss-cn-shenzhen.aliyuncs.com local-path: .
